interface:
  display_name: "ArtAgents"
  short_description: "Video pipeline and GPT Image asset tooling"
  default_prompt: "Use $artagents to generate a Reigh-compatible edit or create GPT Image assets for an ArtAgents run."
policy:
  allow_implicit_invocation: true
