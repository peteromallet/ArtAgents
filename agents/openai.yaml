interface:
  display_name: "Astrid"
  short_description: "Video pipeline and GPT Image asset tooling"
  default_prompt: "Use $astrid to generate a Reigh-compatible edit or create GPT Image assets for an Astrid run."
policy:
  allow_implicit_invocation: true
