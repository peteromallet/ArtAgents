{
  "cache": {
    "mode": "none"
  },
  "command": {
    "argv": [
      "{python_exec}",
      "-m",
      "artagents.executors.understand.run",
      "--mode",
      "{mode}"
    ]
  },
  "description": "Dispatch to the audio, visual, or video understanding executor based on --mode.",
  "id": "builtin.understand",
  "inputs": [
    {
      "description": "Understanding modality: audio | image | visual | video. Determines which underlying executor receives the call.",
      "name": "mode",
      "required": true,
      "type": "string"
    }
  ],
  "isolation": {
    "mode": "subprocess"
  },
  "kind": "built_in",
  "metadata": {
    "runtime_entrypoint": "main",
    "runtime_file": "run.py",
    "runtime_module": "artagents.executors.understand.run"
  },
  "name": "Understand",
  "version": "1.0"
}
