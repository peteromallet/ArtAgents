{
  "cache": {
    "always_run": false,
    "mode": "sentinel",
    "per_brief": false,
    "sentinels": [
      "shots.json"
    ]
  },
  "conditions": [
    {
      "input": "video",
      "kind": "requires_input"
    }
  ],
  "description": "ArtAgents pipeline step: shots.",
  "graph": {
    "consumes": [],
    "depends_on": [
      "builtin.transcribe",
      "builtin.scenes",
      "builtin.quality_zones"
    ],
    "provides": [
      "shots"
    ]
  },
  "id": "builtin.shots",
  "inputs": [
    {
      "description": "Source video to split into shots.",
      "name": "video",
      "required": true,
      "type": "file"
    },
    {
      "description": "Existing scenes.json input.",
      "name": "scenes",
      "required": false,
      "type": "file"
    }
  ],
  "isolation": {
    "binaries": [],
    "mode": "subprocess",
    "network": false,
    "requirements": []
  },
  "kind": "built_in",
  "metadata": {
    "command_builder": "artagents.pipeline.build_pool_steps",
    "pipeline_step": "shots",
    "pipeline_step_order": 3,
    "runtime_entrypoint": "main",
    "runtime_file": "run.py",
    "runtime_module": "artagents.executors.shots.run"
  },
  "name": "Shots",
  "outputs": [
    {
      "description": "",
      "mode": "create_or_replace",
      "name": "shots",
      "path_template": "{out}/shots.json",
      "type": "file"
    }
  ],
  "version": "1.0"
}
