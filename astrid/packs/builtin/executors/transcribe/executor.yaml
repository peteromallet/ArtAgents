{
  "schema_version": 1,
  "cache": {
    "always_run": false,
    "mode": "sentinel",
    "per_brief": false,
    "sentinels": [
      "transcript.json"
    ]
  },
  "clip_kinds_supported": [
    "video",
    "audio"
  ],
  "conditions": [
    {
      "input": "audio",
      "kind": "requires_input"
    }
  ],
  "description": "Transcribe source audio into transcript.json.",
  "graph": {
    "consumes": [],
    "depends_on": [],
    "provides": [
      "transcript"
    ]
  },
  "id": "builtin.transcribe",
  "inputs": [
    {
      "description": "Audio or video-derived audio to transcribe.",
      "name": "audio",
      "required": true,
      "type": "file"
    },
    {
      "description": "Optional environment file for API credentials.",
      "name": "env_file",
      "required": false,
      "type": "file"
    }
  ],
  "isolation": {
    "binaries": [],
    "mode": "subprocess",
    "network": false,
    "requirements": []
  },
  "keywords": [
    "whisper",
    "audio",
    "transcribe",
    "text",
    "speech",
    "transcript"
  ],
  "kind": "external",
  "metadata": {
    "command_builder": "astrid.packs.builtin.orchestrators.hype.run.build_pool_steps",
    "pipeline_step": "transcribe",
    "pipeline_step_order": 0,
    "runtime_entrypoint": "main",
    "runtime_file": "run.py",
    "runtime_module": "astrid.packs.builtin.executors.transcribe.run"
  },
  "name": "Transcribe",
  "outputs": [
    {
      "description": "",
      "mode": "create_or_replace",
      "name": "transcript",
      "path_template": "{out}/transcript.json",
      "type": "file"
    }
  ],
  "pipeline_requirements": [
    "source_audio"
  ],
  "runtime": {
    "command": {
      "argv": [
        "{python_exec}",
        "-m",
        "astrid.packs.builtin.executors.transcribe.run",
        "--audio",
        "{audio}",
        "--out",
        "{out}"
      ]
    },
    "type": "command"
  },
  "short_description": "Transcribe source audio to transcript.json via Whisper.",
  "version": "1.0"
}
