{
  "id": "example-card",
  "kind": "effect",
  "pack_id": "builtin",
  "short_description": "One-sentence verb-first element summary (≤120 chars).",
  "description": "Longer description of the reusable render building block (≤500 chars).",
  "keywords": ["example", "card", "text"],
  "metadata": {
    "label": "Example Card",
    "description": "Describe the reusable render building block.",
    "whenToUse": "Use when a timeline needs this specific visual primitive."
  },
  "schema": {
    "type": "object",
    "required": ["content"],
    "properties": {
      "content": {
        "type": "string"
      }
    }
  },
  "defaults": {
    "content": ""
  },
  "dependencies": {
    "js_packages": [],
    "python_requirements": []
  }
}
