{
  "schema_version": 1,
  "id": "builtin.example",
  "name": "Example",
  "kind": "external",
  "version": "1.0",
  "short_description": "One-sentence verb-first workflow summary (≤120 chars).",
  "description": "Longer description of the workflow this orchestrator coordinates (≤500 chars).",
  "keywords": ["example", "workflow"],
  "runtime": {
    "type": "command",
    "command": {
      "argv": [
        "{python_exec}",
        "-m",
        "astrid.packs.builtin.orchestrators.example.run",
        "{orchestrator_args}"
      ]
    }
  },
  "child_executors": [
    "builtin.example_step"
  ],
  "child_orchestrators": [],
  "cache": {
    "mode": "none"
  },
  "metadata": {
    "runtime_module": "astrid.packs.builtin.orchestrators.example.run",
    "runtime_file": "run.py"
  }
}
